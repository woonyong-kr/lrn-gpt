{
  "hypothesis": "For the seed404 mish baseline that overfit at stride 24, reducing learning_rate from 0.0003 to 0.000275 will slow train-side over-progress enough to reduce the generalization gap without the validation-loss penalty seen from stride16 or max_steps80.",
  "rationale": "Runs 088-090 isolate seed404 as a useful stress case: default stride24 overfit with final_val_loss 5.548481 and overfit_score 0.176580, stride16 rescued the gap but worsened validation to 5.555461, and max_steps80 kept stride24 but worsened validation further to 5.559827 while still overfitting. A small learning-rate reduction keeps the data geometry and training budget fixed, directly testing whether optimization pace can rescue the seed before using stride16 as the default rescue knob.",
  "changed_variables": {
    "learning_rate": 0.000275
  },
  "fixed_variables": {
    "vocab_size": 600,
    "context_length": 48,
    "stride": 24,
    "batch_size": 8,
    "weight_decay": 0.01,
    "grad_clip": 1.0,
    "emb_dim": 128,
    "n_heads": 4,
    "n_layers": 2,
    "drop_rate": 0.12,
    "qkv_bias": false,
    "ffn_mult": 3,
    "norm_first": false,
    "norm_eps": 0.00001,
    "activation_name": "mish",
    "ffn_dropout_position": "none",
    "attention_impl": "sdpa",
    "tie_embeddings": true,
    "init_std": 0.02,
    "max_steps": 90,
    "seed": 404
  },
  "expected_result": "Success means final_val_loss stays near the seed404 default run rather than the stride16 or max_steps80 penalty, ideally <= 5.552, while overfit_score drops clearly below 0.176580 and preferably below 0.08. If validation rises toward 5.556+ or overfit_score remains high, learning-rate reduction alone is not a good rescue.",
  "next_if_success": "Repeat the 0.000275 learning-rate rescue on seed303 to test whether it generalizes across overfit-prone seeds before changing the default plan.",
  "next_if_overfit": "Keep stride16 as the confirmed rescue knob for high-gap seeds, and test a small regularization-side change such as drop_rate 0.14 on seed404 while preserving stride24 and max_steps90.",
  "config_overrides": {
    "vocab_size": 600,
    "context_length": 48,
    "stride": 24,
    "batch_size": 8,
    "learning_rate": 0.000275,
    "weight_decay": 0.01,
    "grad_clip": 1.0,
    "emb_dim": 128,
    "n_heads": 4,
    "n_layers": 2,
    "drop_rate": 0.12,
    "qkv_bias": false,
    "ffn_mult": 3,
    "norm_first": false,
    "norm_eps": 0.00001,
    "activation_name": "mish",
    "ffn_dropout_position": "none",
    "attention_impl": "sdpa",
    "tie_embeddings": true,
    "init_std": 0.02,
    "max_steps": 90,
    "seed": 404
  }
}
