# 한글 특화 Vocab 평가

## 참고 자료

- 슬라이드/자료 링크: [한글 특화 vocab Google Sheet](https://docs.google.com/spreadsheets/d/1L4mbuFxF8joc8KmAhfnrP1CaOEdDkHRrbgDcfDJOqD0/edit?hl=ko&gid=25172778#gid=25172778)

필요시 위 링크를 열어 각 시트의 vocab 원본과 토큰 예시를 직접 확인하면 된다.

## 결론

한글 특화 vocab 중에서는 `min_freq=10`으로 생성한 vocab이 기존 `BPE 3000` vocab보다 한글을 더 의미 있게 토크나이징한다고 평가할 수 있다.

반면 `min_freq=30`, `min_freq=50` vocab은 병합 기준이 너무 강해서 한글 다글자 토큰이 충분히 만들어지지 않았다. 따라서 이 두 버전은 기존 BPE보다 낫다고 보기 어렵다.

전체적인 평가는 다음 순서가 타당하다.

```text
한글 특화 vocab(min_freq=10) > 기본 BPE 3000 > 한글 특화 vocab(min_freq=30) > 한글 특화 vocab(min_freq=50)
```

## 평가 기준

좋은 한글 토크나이저라면 다음 조건을 만족해야 한다.

- 한글이 byte 단위로 많이 쪼개지지 않아야 한다.
- 한 글자 토큰만 많기보다 `영화`, `재미`, `합니다`, `개인적으로` 같은 다글자 chunk가 많아야 한다.
- 자주 나오는 조사, 어미, 표현이 자연스럽게 묶여야 한다.
- UTF-8 깨짐이나 byte fallback 의존이 적어야 한다.
- 같은 문장을 인코딩했을 때 평균 토큰 수가 줄어야 한다.

이번 비교에서는 Google Sheet의 vocab 목록에서 `decoded_text_escaped` 값을 기준으로 한글 토큰 품질을 확인했다.

## 정량 비교

| vocab | 깨끗한 한글 토큰 수 | 2글자 이상 한글 토큰 | 3글자 이상 한글 토큰 | 평균 한글 음절 길이 |
|---|---:|---:|---:|---:|
| 한글 특화 vocab(min_freq=50) | 589 | 163 | 24 | 1.297 |
| 한글 특화 vocab(min_freq=30) | 905 | 325 | 58 | 1.404 |
| 한글 특화 vocab(min_freq=10) | 2208 | 1207 | 310 | 1.702 |
| 기본 BPE 3000 | 2064 | 1087 | 266 | 1.664 |

`한글 특화 vocab(min_freq=10)`은 기본 BPE 3000보다 깨끗한 한글 토큰 수, 2글자 이상 토큰 수, 3글자 이상 토큰 수, 평균 한글 음절 길이에서 모두 더 좋다.

특히 다음 결과가 중요하다.

```text
한글 특화 vocab(min_freq=10)의 깨끗한 한글 토큰: 2208개
기본 BPE 3000의 깨끗한 한글 토큰: 2064개
겹치는 토큰: 2064개
```

즉 기본 BPE 3000에 들어 있는 깨끗한 한글 토큰은 전부 `한글 특화 vocab(min_freq=10)`에도 들어 있고, `한글 특화 vocab(min_freq=10)`은 거기에 추가로 더 많은 한글 토큰을 가지고 있다.

## 한글 특화 vocab(min_freq=10)에 추가로 포함된 의미 있는 토큰 예시

기본 BPE 3000에는 없고 `한글 특화 vocab(min_freq=10)`에만 있는 긴 한글 토큰 예시는 다음과 같다.

```text
킬링타임용
이라고 생각
대한민국
배우들이
보는내내
사람들은
사랑스러
생각하게
재미가 없
할아버지
마지막에
최악의
충분히
한마디
```

이런 토큰들은 한국어 리뷰 문장에서 자주 등장할 수 있는 의미 단위다. 따라서 `한글 특화 vocab(min_freq=10)`은 기본 BPE보다 한국어 문장을 덜 잘게 쪼갤 가능성이 높다.

## 해석

`한글 특화 vocab(min_freq=50)`, `한글 특화 vocab(min_freq=30)`은 `min_freq` 기준이 높아서 자주 등장하는 짧은 한글 조각 위주로 vocab이 구성된 것으로 보인다. 그래서 한글 토큰은 존재하지만 대부분 한 글자 단위에 가깝고, 의미 있는 다글자 chunk가 부족하다.

반면 `한글 특화 vocab(min_freq=10)`은 더 낮은 빈도 기준까지 병합을 허용하면서 한국어에서 자주 쓰이는 표현, 어미, 명사구가 vocab에 더 많이 들어왔다. 이 때문에 기본 BPE 3000보다 한글 친화적인 vocab이라고 평가할 수 있다.

## 한계

이번 평가는 vocab 목록 자체를 기준으로 한 분석이다. 최종적으로 토크나이저 성능을 확인하려면 같은 한국어 평가 문장들을 두 토크나이저로 직접 encode해야 한다.

추가로 확인하면 좋은 지표는 다음과 같다.

- 문장당 평균 토큰 수
- byte fallback 발생 횟수
- decode 후 원문 복원 정확도
- 조사와 어미가 부자연스럽게 분리되는 비율
- 긴 한국어 문장에서 토큰 수가 얼마나 줄어드는지

따라서 현재 vocab 기준으로는 `한글 특화 vocab(min_freq=10)` 설계가 가장 적절해 보이지만, 최종 판단은 실제 한국어 문장 인코딩 결과까지 함께 확인하는 것이 좋다.
